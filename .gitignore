# nothing from CRYPTONITE ever belongs in this repo — see README
.DS_Store
Thumbs.db
*.tmp
*.json.tmp
node_modules/

# ── NOT part of the website ────────────────────────────────────────────────
# Cloudflare Pages serves EVERY file in the output directory, so anything
# committed here becomes a public URL. TWEETS.md was readable at
# /TWEETS.md — the entire unposted launch thread. DEPLOY.md advertised the
# private cryptonite folder and what it holds. These stay on disk; they just
# stop being published. README.md is deliberately kept: it is public-facing.
TWEETS.md
DEPLOY.md
deploy.bat
serve.bat
setup-git.bat
scrub-history.bat
# GATE-AND-DOMAIN.md states the access phrase in plain text and explains how the
# gate can be bypassed. Publishing it would have made /GATE-AND-DOMAIN.md a public
# URL handing out both — the exact shape of the TWEETS.md leak above.
GATE-AND-DOMAIN.md
data/
